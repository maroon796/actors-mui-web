import { Box } from '@mui/material';

const Feed = () => {
  return (
    <Box bgcolor="gold" flex={4} p={2} sx={{ marginLeft: { xs: 30, sm: 20 } }}>
      Feed
    </Box>
  );
};

export default Feed;
