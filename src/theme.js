import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#7FBCD2',
    },
    secondary: {
      main: '#A5F1E9',
      light: '#E1FFEE',
    },
    otherColor: {
      main: '#FFEEAF',
    },
  },
});
